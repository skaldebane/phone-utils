_phone() {
    local cur prev words cword
    _init_completion || return
    
    case "$prev" in
        phone)
            COMPREPLY=($(compgen -W "refresh tether mtp" -- "$cur"))
            ;;
    esac
}

complete -F _phone phone
