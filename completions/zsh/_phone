#compdef phone

_phone() {
    _values 'commands' \
        'refresh[Force network re-registration via 2G→LTE]' \
        'tether[Enable USB RNDIS tethering]' \
        'mtp[Disable tethering, set USB to MTP]' \
        'hotspot[Manage WiFi hotspot (on/off/status/config)]'
}
